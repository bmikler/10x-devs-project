import { describe, it } from "vitest";

// Placeholder so the integration lane resolves before Phase 3 adds data-isolation.test.ts.
// Remove once tests/integration/data-isolation.test.ts exists.
describe("integration lane", () => {
  it("config resolves", () => {
    // intentionally empty — proves the lane boots
  });
});
